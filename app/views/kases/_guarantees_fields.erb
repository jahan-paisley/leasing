<% @kase.contract.guarantees.each_with_index do |guarantee, index| %>
    <%= contract_form.fields_for :guarantees, guarantee do |f| %>
        <div class="field">
          <%= f.label :amount %>
          <%= f.number_field :amount %>
        </div>
        <div class="field">
          <%= f.label :description %>
          <%= f.text_field :description %>
        </div>
    <% end %>
<% end %>
